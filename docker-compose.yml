version: '3.8'

services:
  web:
    build: .
    container_name: fastapi_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: always
    networks:
      - app-network

networks:
  app-network:
    name: app-network
    driver: bridge 